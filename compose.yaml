include:
  - auxiliary.yaml
  - media-acquisition.yaml
  - media-server.yaml
  - reverse-proxy.yaml

networks:
  internal-containers:
    driver: bridge
    ipam:
      config:
        - subnet: 172.18.0.0/16
          gateway: 172.18.0.1